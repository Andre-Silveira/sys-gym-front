import Title from "../../components/common/Title";

type Props = {};

const ListaAlunos = (props: Props) => {
  return (
    <div>
      <Title titulo={"Alunos"} />
      ListaAlunos
    </div>
  );
};

export default ListaAlunos;